---
# TODO_MANUAL: mapear variaveis de ambiente reais da aplicacao
- name: Render env file
  ansible.builtin.copy:
    dest: "/opt/{{ app_name }}/.env"
    content: |
      APP_NAME={{ app_name }}
      DB_HOST={{ db_conn }}
      DB_NAME={{ db_name }}
